Overview
========

.. code:: console

   $ pacz --help
   Usage: pacz [-j] CARRIER IDENT

   Carriers:
     allegro (allegro.pl)
     dpd (dpd.com.pl)
     gls (gls-group.eu)
     inpost (inpost.pl)
     orlen (orlenpaczka.pl paczkawruchu)
     poczta (poczta-polska.pl poczta-polska)
     pointpack (pointpack.pl)
     poltraf (poltraf.pl)
     swiss-post (post.ch)

   Options:
     -j, --json  prefer JSON (not YAML) for printing
                 machine-readable data
     -h, --help  show this help message and exit

Requisites
==========

* Perl ≥ 5.14

* `HTTP::CookieJar <https://metacpan.org/dist/HTTP-CookieJar>`_

* `YAML::XS <https://metacpan.org/dist/YAML-LibYAML>`_ (recommended)

.. vim:ts=3 sts=3 sw=3 et ft=rst
